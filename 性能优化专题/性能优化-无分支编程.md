# 1、无分支编程是什么？
无分支编程为并行化处理尤其是SIMD提供了可能，这里节选【https://graphics.stanford.edu/~seander/bithacks.html#BranchlessConditionalAssignment】里面常用、易用的部分做下记录

# 2、常用的无分支编程技巧
```C
/**

 * 文件功能：无分支编程的代码示例

 * 文件链接：https://graphics.stanford.edu/~seander/bithacks.html#BranchlessConditionalAssignment

 * 文件类型：常用的无分支编程技巧、学习使用

 * 文件作者：ZFH/z00465944

 * **/

#include <stdio.h>

#include <stdlib.h>

#include <string.h>

#include <math.h>

#include <stdint.h>

#include <stdbool.h>





#define CHAR_BIT 8



// 求整数的符号位

void ComputeTheSignOfAnInteger() {

    int v; // 求v的的符号数（+/-）

    int sign; // 保存结果



    // if v < 0 then -1 else 0

    sign = -(v < 0); 

    sign = -(int)((unsigned int)((int) v) >> (sizeof(int) * CHAR_BIT - 1));

    sign = v >> (sizeof(int) * CHAR_BIT -1);



    // if v < 0 then -1 else +1

    sign = +1 | (v >> (sizeof(int) * CHAR_BIT - 1));



    // if v == 0 then 0 elseif v > 0 then +1 elae -1

    sign = (v != 0) | -(int)((unsigned int)((int)v) >> (sizeof(int) * CHAR_BIT - 1));

    sign = (v != 0) | (v >> (sizeof(int) * CHAR_BIT - 1));

    sign = (v > 0) - (v < 0);

    

    // if v < 0 then 0 else +1

    sign = 1 ^ ((unsigned int)v >> (sizeof(int) * CHAR_BIT - 1));

}



// 检测两个整数是否具有相反的符号

void DetectIfTwoIntegersHaveOppositeSigns()

{

    int x, y;

    bool f = ((x ^ y) < 0); // 当且仅当x和y正负号不同时为真

}

// 无分支计算整数的绝对值

void ComputerTheIntegerABSWithoutBranch()

{

    int v; // 求v的绝对值

    unsigned int r; // 保持绝对值

    

    int const mask = v >> sizeof(int) * CHAR_BIT - 1;



    r = (v + mask) ^ mask;

}

// 求解两个整数的最大/最小值

void ComputerTheMinAndMax()

{

    int x , y; // 待处理的整数

    int r; // 结果值

    

    // min(x, y)

    r = y ^ ((x ^ y) & -(x < y));

    // max(x, y)

    r = x ^ ((x ^ y) & -(x < y));

    // If you know that INT_MIN <= x - y <= INT_MAX, then you can use the following, 

    // which are faster because (x - y) only needs to be evaluated once

    // min(x, y)

    r = y + ((x - y) & ((x - y) >> (sizeof(int) * CHAR_BIT - 1))); 

    // max(x, y)

    r = x - ((x - y) & ((x - y) >> (sizeof(int) * CHAR_BIT - 1))); 

}

// 判断整数是否为2的幂

void DeterminingIfAnIntegerIsAPowerOf2()

{

    unsigned int v;

    bool f;



    f = (v & (v - 1)) == 0;

    // Note that 0 is incorrectly considered a power of 2 here. To remedy this, use

    f = v && !(v & (v - 1));

}

// 有条件设置或清除位，而不进行分支

void ConditionallySetOrClearBitsWithoutBranching()

{

    bool f;         // conditional flag

    unsigned int m; // the bit mask

    unsigned int w; // the word to modify:  if (f) w |= m; else w &= ~m; 



    w ^= (-f ^ w) & m;



    // OR, for superscalar CPUs:

    w = (w & ~m) | (-f & m);

}

// 有条件地否定一个值，而不进行分支

void ConditionallyNegateAValueWithoutBranching()

{

    bool fDontNegate;  // Flag indicating we should not negate v.

    int v;             // Input value to negate if fDontNegate is false.

    int r;             // result = fDontNegate ? v : -v;

    

    r = (fDontNegate ^ (fDontNegate - 1)) * v;

    // If you need to negate only when a flag is true, then use this:

    bool fNegate;  // Flag indicating if we should negate v.

    int v;         // Input value to negate if fNegate is true.

    int r;         // result = fNegate ? -v : v;



    r = (v ^ -fNegate) + fNegate;

}

// 根据掩码合并两个值的位

MergeBitsFromTwoValuesAccordingToAMask()

{

    unsigned int a;    // value to merge in non-masked bits

    unsigned int b;    // value to merge in masked bits

    unsigned int mask; // 1 where bits from b should be selected; 0 where from a.

    unsigned int r;    // result of (a & ~mask) | (b & mask) goes here



    r = a ^ ((a ^ b) & mask); 

}

// 计算整数中的1的个数

void CountingBitsSet()

{

    unsigned int v; // count the number of bits set in v

    unsigned int c; // c accumulates the total bits set in v

    for (c = 0; v; v >>= 1)

    {

        c += v & 1;

    }

    // Counting bits set, Brian Kernighan's way

    for (c = 0; v; c++)

    {

        v &= v - 1; // clear the least significant bit set

    }

}

// BIT翻转

void ReverseBitsTheObviousWay() 

{

    unsigned int v;     // input bits to be reversed

    unsigned int r = v; // r will be reversed bits of v; first get LSB of v

    int s = sizeof(v) * CHAR_BIT - 1; // extra shift needed at end



    for (v >>= 1; v; v >>= 1)

    {   

        r <<= 1;

        r |= v & 1;

        s--;

    }

    r <<= s; // shift when v's highest bits are zero

}

```
