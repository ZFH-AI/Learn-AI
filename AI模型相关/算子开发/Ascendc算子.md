
# 1、编程模型

1.1、SIMD（Single Instruction Multiple Data）：单指令多数据，通过单条指令多个数据的方式实现并行计算

1.2、SIMT（Single Instruction Multiple Thread）：单指令多线程，通过单条指令多个线程的方式实现并行计算

```shell

|  编程模式    |  计算空间    |  特点
--------------------------------------------------------------------------------------------------------
| SIMD编程     |  AI CORE     | 适合矩阵计算，连续计算的矢量算子及融合算子场景，提供SIMD和SIMT混合的方式
--------------------------------------------------------------------------------------------------------
| SIMT编程     |  AI CORE     | 适用于离散访问场景，复杂分支控制场景
--------------------------------------------------------------------------------------------------------
| AI CPU编程   |  AI CPU      | 作为AI CORE计算的补充
--------------------------------------------------------------------------------------------------------
```

## 1.1、AI CORE SIMD编程
### 1.1.1、抽象硬件架构

```shell
  +------------------------------------------------------------------------------------------------------
  |                +-------------------------+                ---> 指令流程                             |
  |                |  算子指令序列           |                ***> 数据流程                             |
  |                +-------------------------+                ###> 同步流程                             |
  |                         |                                                                           |
  |                         |                                                                           |
  |                         V                                                                           |
  |       +--------------------------------------+                                                      |
  |       |  Scalar 计算单元                     |                                                      |
  |       +--------------------------------------+                                                      |
  |        *               |    #                                                                       |
  |        *               |    # # # # # # # # # # # # # # # # # # # # # # # # # # # # #               |
  |        *               |----#---------------------------#---------------------      #               |
  |        *               V    V                      V    V                    V      V               |
  |        *             指令 +----------------+     指令 +--------------+      指令 +--------------+   |
  |        *             队列 | Vector计算单元 |     队列 | Cube计算单元  |     队列 | DMA搬运单元   |   |
  |        *                  +----------------+          +--------------+          +---------------+   |
  |        *                         *                         *                          *       *     |
  |        * * * * *  *              *                         *                          *       *     |
  |        *          V              V                         V                          V       *     |
  |        *     +---------------------------------------------------------------------------+    *     |
  |        *     |      Laocl Memeory                                                        |    *     |
  |        *     +---------------------------------------------------------------------------+    *     |
  +--------*--------------------------------------------------------------------------------------*------
           *                                                                                      *
           V                                                                                      V
  +------------------------------------------------------------------------------------------------------+
  |       Global Memory                                                                                  |
  +------------------------------------------------------------------------------------------------------+
  
```
AI CORE 内部核心组件
```shell

|  组件分类    |  组件名称         |  组件功能
------------------------------------------------------------------------------------------------------
| 计算单元     | Scalar            |  执行地址计算，循环控制等标量计算工作，并把向量计算、矩阵计算、数据搬运、
|              |                   |  同步指令发射给对应的单元执行
|              ----------------------------------------------------------------------------------------
|              | Vector            | 负责执行向量运算
|              ----------------------------------------------------------------------------------------
|              | Cube              | 负责执行矩阵运算
-------------------------------------------------------------------------------------------------------
|  存储单元    | Local Memory      | AI CORE 的内部存储
-------------------------------------------------------------------------------------------------------
|  搬运单元    | DMA(Direct Memory | 负责内存搬运，包括Global Memory和Local Memory之间的数据搬运以及
|              | Acccess)          | 不同层级Local Memory之间的数据搬运
--------------------------------------------------------------------------------------------------------
```
### 1.1.2、核函数

存储单元包括内部存储和外部存储：

1、AI Core的内部存储，统称为Local Memory，对应的数据类型为LocalTensor。

2、AI Core能够访问的外部存储称之为Global Memory，对应的数据类型为GlobalTensor。

区别于普通的C++函数调用时仅执行一次，当核函数被调用时，多个核都执行相同的核函数代码，具有相同的函数入参，并行执行

```C

 // 函数类型限定符__global__和__aicore__
 // 其指针入参变量需要增加变量类型限定符__gm__，表明该指针变量指向Global Memory上某处内存地址
 // 使用<<<...>>>内核调用符调用执行核函数，并指定调用时的执行核数

// 实现核函数
__global__ __aicore__ void add_custom(__gm__ uint8_t* x, __gm__ uint8_t* y, __gm__ uint8_t* z)
{
  // Other
}

// 调用核函数
void add_custom_do(uint32_t numBlocks, void* l2ctrl, void* stream, uint8_t* x, uint8_t* y, uint8_t* z)
{
    // numBlocks: 规定了核函数将会在几个核上执行。每个执行该核函数的核会被分配一个逻辑ID，即block_idx，
    //  可以在核函数的实现中调用GetBlockIdx来获取block_idx

    // l2ctrl: 保留参数，暂时设置为固定值nullptr

    // stream:类型为aclrtStream，stream用于维护一些异步操作的执行顺序，确保按照应用程序中的代码调用顺序
    // 在device上执行
    add_custom<<<numBlocks, l2ctrl, stream>>>(x, y, z);
}


+-------------------------------------+                  +---------------------------------------------------------+
| Host                                |                  |  Device                                         
|                                     | <<<>>>           | 
|    host侧执行函数                   |----------------->|  核函数
|  void func_host(argument list)      |                  |  __glboal__ __aicore__ void kernel_name(argument_list)
|                                     |                  |
+-------------------------------------+                  |  device侧执行函数
                                                         |  __aicore__ void dunc_device(argumen_list)
                                                         |
                                                         +-----------------------------------------------------------+
```


## 1.2、AI CORE SIMT

### 1.2.1、SIMT编程模型的线程层次结构分为两层

1、线程块网格（grid）：有多个线程块（Thread Block）组成，使用内置变量gridDim来表示启用的线程块数量，同一个时刻AIV核只执行一个线程块任务

2、线程块（Thread Block）：由若干线程（Thread）组成，使用内置变量blockDim表一个线程启用的线程个数，一个线程块最多可以启用2048个线程

基于SIMT编程模型的程序，在AIV核上执行多个结构相同的线程块，执行的总线程数等于gridDim*blockDim

```shell

Grid

    Thread Block               Thread Block                Thread Block               Thread Block          
+---------------------+      +---------------------+     +---------------------+      +---------------------+
|                     |      |                     |     |                     |      |                     |
|  |  |  |  |  |  |   |      |  |  |  |  |  |  |   |     |  |  |  |  |  |  |   |      |  |  |  |  |  |  |   |   
|  V  V  V  V  V  V   |      |  V  V  V  V  V  V   |     |  V  V  V  V  V  V   |      |  V  V  V  V  V  V   |
|                     |      |                     |     |                     |      |                     |
+---------------------+      +---------------------+     +---------------------+      +---------------------+


gridDim由三维结构dim3来表示，{dimx，dimy，dimz}用于指定3个不同维度的线程块的大小，三维乘积的总数不超过65535，
各线程块可通过线程块索引blockIdx进行标识。blockDim也由dim3三维结构表示，三维乘积的总数不超过2048，
各线程可通过线程块内线程索引threadIdx进行标识。线程索引的计算示例如下图所示


blockDim = 6
    Thread Block               Thread Block                Thread Block               Thread Block          
+---------------------+      +---------------------+     +---------------------+      +---------------------+
|                     |      |                     |     |                     |      |                     |
|  |  |  |  |  |  |   |      |  |  |  |  |  |  |   |     |  |  |  |  |  |  |   |      |  |  |  |  |  |  |   |   
|  V  V  V  V  V  V   |      |  V  V  V  V  V  V   |     |  V  V  V  V  V  V   |      |  V  V  V  V  V  V   |
|                     |      |                     |     |              *      |      |                     |
+---------------------+      +---------------------+     +--------------*------+      +---------------------+
                                                                        *
                                                                        *
                                                                        V
                                                              ThreadIdx = 4
                                                              线程索引为： 2 * 6 + 4 = 16

```
层调度过程中，同一时刻一个AIV只能执行一个线程块任务，每个线程块会被切分成多个Warp依次调度并完成执行。Warp是执行相同指令的线程的集合，每个Warp包含32个线程。

每个AIV核包含4个Warp调度器（Warp Scheduler），调度器编号scheduler id为warp id % 4。

### 1.2.2、内存层级

SMIT编程可使用的内存空间包含如下三种

1、每个线程独立的寄存器和栈，用于存储局部变量，可用寄存器数量与线程块中的线程数有关
```shell
| Thread 的个数  | 每个Thread可用寄存器个数
-------------------------------------------
| 1025--2048     | 16
------------------------------------------
| 513--1024      | 32
------------------------------------------
| 257--512       | 64
------------------------------------------
| 1--256         | 127
------------------------------------------
```
2、线程块内所有线程共享的本地内存，即Unified Buffer 该内存区域由线程块内所有线程共同访问，且生命周期和线程块一致

3、所有线程均可直接访问的全局内存，即Global Memory
```shell
        +---------------+
        |     Thread    | <------------  每个线程的寄存器和栈
        +---------------+
               |
               |
               V
            Thread Block                
     +---------------------+      
     |                     |      
     |  |  |  |  |  |  |   | <-----------  每个线程块的共享内存UB      
     |  V  V  V  V  V  V   |     
     |                     |      
     +---------------------+
               |
               |
               V
     +--------------------+
     |  GM                |<--------------  所有线程共享的全局内存GM
     +--------------------+

```

Unified Buffer内存空间总大小为256KB，按功能划分为四个主要区域，从低地址向高地址依次为静态内存、动态内存、 预留空间、Data Cache

```shell
   0
   |     +-----------+
   |     | 静态内存  |
   |     +-----------+
   |     | 动态内存  |
   |     +-----------+
   |     | 预留空间  |
   |     +-----------+
   |     | data cache|
   |     +-----------+
   V
  256K

```

### 1.2.3、核函数
Ascend C支持开发者自定义核函数来扩展C++，核函数在AI处理器上执行时实际是若干线程在并行执行，每个线程有独立的寄存器和栈，共同完成数据计算任务


```shell
// 使用函数类型限定符__global__， 标识它是一个核函数。
__global__ void kernel_name(argument_list)
```
Host侧通过内核调用符<<<...>>>的语法形式调用核函数，如下所示：


```shell
参数1：numBlocks：为核函数配置的线程块的个数，即启用的核数, 支持int32_t和dim3类型
参数2：threadsPerBlock：每个线程块内并发执行的线程数量，支持int32_t和dim3类型
参数3：dynUBufSize：动态申请内存空间总大小，一般情况设置为0
参数4：stream：用于host侧和device侧的流同步
kernel_name<<<numBlocks, threadsPerBlock, dynUBufSize, stream>>>(args...)
```

## 1.2、AI CPU

```shell
// 调用printf接口需要包含的头文件
#include "aicpu_api.h"

__global__ __aicpu__ uint32_t hello_world(void *args)
{
    AscendC::printf("Hello World!!!\n");
    return 0;
}
```







