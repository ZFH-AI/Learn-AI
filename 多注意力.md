
参考：https://www.51cto.com/aigc/5021.html

MHA、MQA、GQA和MLA分别代表了注意力机制在不同方向上的优化。MHA适合高表达能力的场景，MQA适合推理速度要求极高的场景，GQA在两者之间找到平衡，

而MLA则在高效推理和高质量输出方面表现最佳

```shell

|  机制  | KV缓存需求  |  推理速度  |  模型质量  |  适应场景                    |
--------------------------------------------------------------------------------
| MHA    | 高          | 较慢      |  高        | 需要高表达能力的场景          |
--------------------------------------------------------------------------------
| MQA    | 极低        | 最快      | 较低       | 推理速度要求极高的场景        |
--------------------------------------------------------------------------------
| GQA    | 较低        | 较快      | 较高       | 平衡推理速度和模型质量的场景  |
--------------------------------------------------------------------------------
| MLA    | 最低        | 高        | 最高       | 高效推理与高质量输出的场景    |
--------------------------------------------------------------------------------
```


# 1、 MHA：Multi Head Attention 多头注意力

```shell
MHA： Multi Head Attention

values    |     |     |

Key       |     |     |

Queries   |     |     |

```

## 原理与公式
MHA是TF架构的核心组成部分，其原理是将输入数据通过不同的头进行多次注意力计算，然后将这些计算结果拼接起来，在通过一个线性变换得到最终的输出。这种机制能够从不同的子空间
提取信息，从而捕捉到输入数据中更丰富、更复杂的特征关系。

具体来说，假设输入数据Q：查询向量，K：键向量，V：值向量，每个头的注意力计算公式如下
Attention（Q，K，V） = softmax((QK^T)V / sort(d_K)) , d_k是键向量的维度，用于缩放点积结果，防止梯度消失或爆炸、
在多头注意力中，将输入数据分别通过不同的线性变换得到多个头的QKV，然后多每个头分别进行上述注意力计算，最后将所有头的输出拼接起来，并通过一个线性变换得到最终结果
MultiHead（Q，K，V）=Concat（Head1，Head2，...，head_n）W^0 ，其中n是头的数量，W^0是输出的线性变换矩阵

## 优点与局限
优点

1、能够捕捉到输入数据中不同位置之间的长距离依赖关系

2、通过多个头并行计算，能够从不同的子空间中提取信息，从未捕捉到更丰富的特征和模式，提高模型的表达能力和泛华能力

局限

1、计算复杂度较高，尤其是输入数据的维度和头的数量较大时，计算量会显著增加，增肌训练和推理时延

2、由于每个头是独立学习，可能会出现一些头学到相似的特征，导致模型的冗余性增加，降低了模型的效率和可解释性



# 2、MQA：Multi Query Attention 分组查询注意力
```shell
MQA： Multi Query Attention

values          |     

Key             |     
           *    *   *
Queries   |     |     |

```

## 原理与公式
多查询注意力机制MQA是多头注意力机制的一种改进版本，只在减少计算复杂度和内存占用，同时保持模型的性能，MQA的核心思想是将多个查询向量共享一组键向量和值向量，而不是为每个
查询向量都独立计算一组键向量和值向量

具体来说：假设输入数据Q：查询向量，K：键向量，V：值向量，MQA计算过程如下

1、首先将查询向量Q 通过一个线性变换得到多个查询头{Q1，Q2，..., Qn}，其中n是查询头的数量

2、将K和V通过一个共享的线性变换得到一组{K}和{V}

3、对每个查询头分别进行注意力计算
   Attention（Qi，K，V） = softmax((QiK^T)V / sort(d_K)) , d_k是键向量的维度，用于缩放点积结果，防止梯度消失或爆炸、

4、将所有查询头的输出拼接起来，并通过一个线性换得到最后的结果
   MultiHead（Q，K，V）=Concat（ Attention（Q1，K，V）， Attention（Q2，K，V），...， Attention（Qn，K，V）） W^0 ，其中n是头的数量，W^0是输出的线性变换矩阵


优点

1、减少了K和V的计算和存储开销，提高训练和推理效率


# 3、GQA：Grouped Query Attention 分组查询注意力

```shell
GQA： Grouped Query Attention

values         |         |         |     
 
Key            |         |         |
            *    *     *   *     *   *
Queries     |     |   |     |   |     |

```

## 原理与公式
分组查询注意力机制（GQA）是一种在多头注意力基础上进行优化的机制，旨在进一步提高计算效率和模型性能。GQA的核心思想是将查询向量分组，每组共享一组键向量和值向量，而不是为每个查询头都独立计算键向量和值向量

具体来说：假设输入数据Q：查询向量，K：键向量，V：值向量，GQA计算过程如下

1、首先将Q分为g组，每组包含 h/g 个查询头，其中h是总头数，g是分组数

2、对于每组查询头，共享一组Kg 和 Vg，键向量和值向量通过线性变换得到Kg = W1K，Vg=W2V，其中W1和W2是针对每组的线性变换矩阵

3、对于每组中的查询头 Attention(Qi,Kg,Vg) = softmax(QiKg^t / sort(d_k))Vg , d_k是键向量的维度，用于缩放点积结果，防止梯度消失或爆炸

4、最后将所有查询头的输出拼接起来，通过一个线性变换得到最终结果
   GroupedQuery（Q,K,V）=Concat(Attention(Q1,Kg,Vg)，...， Attention(Qh,Kg,Vg)）W，其中W是输出的线性变换矩阵
   

## 优点与应用
GQA在计算效率和模型性能方面有显著的优势，通过将查询向量分组并共享键向量和值向量，GQA减少了键向量和值向量的计算和存储开销，同时保持了多头注意力机制的性能

1、计算效率提升

2、内存占用减少

3、模型性能保持

# 4、MLA：Multi Head latent Attention 多头潜在注意力

MLA:多头潜在注意力机制是一种新型的注意力机制，旨在进一步优化多头注意力机制的性能和效率，MLA的核心思想是引入潜在空间（Latent Space），通过在潜在空间中进行注意力计算
减少计算复杂度，同时捕捉更丰富的特征关系

## 原理与公式

具体来说：假设输入数据Q：查询向量，K：键向量，V：值向量，MLA计算过程如下 

1、首先将Q和K分别通过线性变换映射到潜在空间，得到潜在查询向量Q^ = WqQ 和 潜在键向量 K^ = WkK 
   其中Wq和Wk是线性变换矩阵

2、在潜在空间中进行注意力计算Attention(Q^,K^,V) = softmax(Q^K^T / sort(d_k))V,  d_k是键向量的维度，用于缩放点积结果，防止梯度消失或爆炸

3、最后将所有的输出拼接取来，并通过一个线性变换得到最终结果
MultiHeadLatent（Q，K，V） = Concat（Attention(Q^,K^,V)，..., Attention(Q^,K^,V)）W， h个头，W是线性变换矩阵

## 优点与应用

1、计算复杂度降低：通过在低维潜在空间中进行注意力计算，MLA显著减少了计算量。实验表明，与传统的多头注意力机制相比，MLA的计算复杂度降低了约 30%，
   这使得模型在大规模数据集上的训练和推理速度更快

2、特征提取能力增强：潜在空间能够捕捉到输入数据中更深层次的特征关系。在自然语言处理任务中，MLA能够更好地理解句子中单词之间的语义关系，从而提高模型的性能。
   例如，在机器翻译任务中，使用 MLA 的模型 BLEU 分数比传统多头注意力模型提高了 5%

3、模型泛华能力提升：MLA通过潜在空间的映射，能够更好地处理不同类型的输入数据，提高了模型的泛化能力。在跨领域任务中，MLA模型的性能表现出色，能够适应不同领域的数据分布

4、内存占用减少：由于在潜在空间中进行计算，MLA减少了键向量和值向量的存储需求


在实际应用中，MLA已经被证明在多种任务中表现出色。例如，在文本分类任务中，MLA模型的准确率达到了 92%，比传统多头注意力模型提高了 3 个百分点。

在图像识别任务中，MLA机制也被应用于视觉 Transformer 中，显著提高了模型的准确性和效率

