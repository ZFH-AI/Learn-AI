
参考：https://www.51cto.com/aigc/5021.html

# 1、 MHA：Multi Head Attention 多头注意力

```shell
MHA： Multi Head Attention

values    |     |     |

Key       |     |     |

Queries   |     |     |

```

## 原理与公式
MHA是TF架构的核心组成部分，其原理是将输入数据通过不同的头进行多次注意力计算，然后将这些计算结果拼接起来，在通过一个线性变换得到最终的输出。这种机制能够从不同的子空间
提取信息，从而捕捉到输入数据中更丰富、更复杂的特征关系。

具体来说，假设输入数据Q：查询向量，K：键向量，V：值向量，每个头的注意力计算公式如下
Attention（Q，K，V） = softmax((QK^T)V / sort(d_K)) , d_k是键向量的维度，用于缩放点积结果，防止梯度消失或爆炸、
在多头注意力中，将输入数据分别通过不同的线性变换得到多个头的QKV，然后多每个头分别进行上述注意力计算，最后将所有头的输出拼接起来，并通过一个线性变换得到最终结果
MultiHead（Q，K，V）=Concat（Head1，Head2，...，head_n）W^0 ，其中n是头的数量，W^0是输出的线性变换矩阵

## 优点与局限
优点

1、能够捕捉到输入数据中不同位置之间的长距离依赖关系

2、通过多个头并行计算，能够从不同的子空间中提取信息，从未捕捉到更丰富的特征和模式，提高模型的表达能力和泛华能力

局限

1、计算复杂度较高，尤其是输入数据的维度和头的数量较大时，计算量会显著增加，增肌训练和推理时延

2、由于每个头是独立学习，可能会出现一些头学到相似的特征，导致模型的冗余性增加，降低了模型的效率和可解释性



# 2、MQA：Multi Query Attention 分组查询注意力
```shell
MQA： Multi Query Attention

values          |     

Key             |     
           *    *   *
Queries   |     |     |

```

## 原理与公式
多查询注意力机制MQA是多头注意力机制的一种改进版本，只在减少计算复杂度和内存占用，同时保持模型的性能，MQA的核心思想是将多个查询向量共享一组键向量和值向量，而不是为每个
查询向量都独立计算一组键向量和值向量

具体来说：假设输入数据Q：查询向量，K：键向量，V：值向量，MQA计算过程如下

1、首先将查询向量Q 通过一个线性变换得到多个查询头{Q1，Q2，..., Qn}，其中n是查询头的数量

2、将K和V通过一个共享的线性变换得到一组{K}和{V}

3、对每个查询头分别进行注意力计算
   Attention（Qi，K，V） = softmax((QiK^T)V / sort(d_K)) , d_k是键向量的维度，用于缩放点积结果，防止梯度消失或爆炸、

4、将所有查询头的输出拼接起来，并通过一个线性换得到最后的结果
   MultiHead（Q，K，V）=Concat（ Attention（Q1，K，V）， Attention（Q2，K，V），...， Attention（Qn，K，V）） W^0 ，其中n是头的数量，W^0是输出的线性变换矩阵


优点

1、减少了K和V的计算和存储开销，提高训练和推理效率


# 3、GQA：Grouped Query Attention

```shell
GQA： Grouped Query Attention

values         |         |         |     
 
Key            |         |         |
            *    *     *   *     *   *
Queries     |     |   |     |   |     |

```

## 原理与公式
分组查询注意力机制（GQA）是一种在多头注意力基础上进行优化的机制，旨在进一步提高计算效率和模型性能。GQA的核心思想是将查询向量分组，每组共享一组键向量和值向量，而不是为每个查询头都独立计算键向量和值向量

## 优点与应用
